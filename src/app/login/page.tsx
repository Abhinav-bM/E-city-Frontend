import LoginForm from "@/components/auth/loginForm";
import MainWrapper from "@/wrapper/main";

const page = () => {
  return (
    <MainWrapper>
      <LoginForm />
    </MainWrapper>
  );
};

export default page;
