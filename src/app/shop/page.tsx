import MainWrapper from "@/wrapper/main";
import ShopPage from "@/page-component/shop";

const page = () => {
  return (
    <MainWrapper>
      <ShopPage />
    </MainWrapper>
  );
};

export default page;
